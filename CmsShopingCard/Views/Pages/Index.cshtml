@model CmsShopingCard.Models.ViewModels.pages.PageVM

@{
    ViewBag.Title = ViewBag.PageTitel;
    TempData["PageTitel"] = ViewBag.PageTitel;
}



@if (Model.Titel != "Home")
{
    @Html.Raw(Model.Body)
}
else
{
    <div class="home-wrap">
        <!-- home slick -->
        <div id="home-slick">
            @foreach (var img in Model.SliderGallery)
            {
                  <!-- banner1 -->
                <div class="banner banner-1">
                    <img src="/Images/Uploads/SliderImages/@img.ImageName" height="610" />
                    <div class="banner-caption text-center">
                        <h1 class=" font-weak" style="color:#F8694A">@img.Title</h1>
                        @if (img.UrlLink != null)
                        {
                            <a class="btn primary-btn" href="@Url.Content(img.UrlLink)">Shop Now</a>
                        }
                    </div>
                </div>
            <!-- /banner -->
            }          
            <!-- /banner -->
        </div>
        <!-- /home slick -->
    </div>

  <!-- section -->
    @*<div class="section">
        <!-- container -->
        <div class="container">
            <!-- row -->
            <div class="row">
                <!-- banner -->
                <div class="col-md-4 col-sm-6">
                    <a class="banner banner-1" href="#">
                        <img src="~/Content/Templet_Files/img/banner10.jpg" />
                        <div class="banner-caption text-center">
                            <h2 class="white-color">NEW COLLECTION</h2>
                        </div>
                    </a>
                </div>
                <!-- /banner -->
                <!-- banner -->
                <div class="col-md-4 col-sm-6">
                    <a class="banner banner-1" href="#">
                        <img src="~/Content/Templet_Files/img/banner11.jpg" />
                        <div class="banner-caption text-center">
                            <h2 class="white-color">NEW COLLECTION</h2>
                        </div>
                    </a>
                </div>
                <!-- /banner -->
                <!-- banner -->
                <div class="col-md-4 col-md-offset-0 col-sm-6 col-sm-offset-3">
                    <a class="banner banner-1" href="#">
                        <img src="~/Content/Templet_Files/img/banner12.jpg" />
                        <div class="banner-caption text-center">
                            <h2 class="white-color">NEW COLLECTION</h2>
                        </div>
                    </a>
                </div>
                <!-- /banner -->

            </div>
            <!-- /row -->
        </div>
        <!-- /container -->
    </div>*@
    <!-- /section -->

    <!--Partial View Get Product By Category-->
 }
